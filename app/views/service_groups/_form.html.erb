<%= form_with(model: service_group, local: true) do |form| %>
  <% if service_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(service_group.errors.count, "error") %> prohibited this service_group from being saved:</h2>

      <ul>
      <% service_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
